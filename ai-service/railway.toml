# LEGADO — no usar para producción. Despliegue actual: Render (GitHub Actions + deploy hook).

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
