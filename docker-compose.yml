services:
  frontend:
    build:
      context: ./calzatura-vilchez
      target: dev
    container_name: calzatura-frontend
    ports:
      - "5173:5173"
    environment:
      VITE_AI_SERVICE_URL: http://localhost:8000
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ./calzatura-vilchez:/app
      - frontend_node_modules:/app/node_modules
    depends_on:
      ai-service:
        condition: service_started
      firebase-emulator:
        condition: service_started

  ai-service:
    build:
      context: ./ai-service
    container_name: calzatura-ai-service
    ports:
      - "8000:8000"
    environment:
      FIREBASE_KEY_PATH: /run/secrets/firebase-service-account.json
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./ai-service:/app
      - ./ai-service/serviceAccountKey.json:/run/secrets/firebase-service-account.json:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3

  dashboard-iso:
    build:
      context: .
      dockerfile: dashboard-iso25000/Dockerfile
    container_name: calzatura-dashboard-iso
    ports:
      - "4321:4321"
    environment:
      ISO_BIND_HOST: "0.0.0.0"
      ISO_PORT: "4321"
    volumes:
      - .:/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://127.0.0.1:4321/stress/"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s

  firebase-emulator:
    build:
      context: .
      dockerfile: firebase-emulator/Dockerfile
    container_name: calzatura-firebase-emulator
    ports:
      - "4000:4000"
      - "5001:5001"
      - "9099:9099"
    environment:
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      AI_SERVICE_URL: ${AI_SERVICE_URL}
      AI_SERVICE_BEARER_TOKEN: ${AI_SERVICE_BEARER_TOKEN}
    volumes:
      - emulator_data:/workspace/emulator-data
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://127.0.0.1:4000/"]
      interval: 15s
      timeout: 10s
      retries: 8
      start_period: 60s

volumes:
  frontend_node_modules:
  emulator_data:
