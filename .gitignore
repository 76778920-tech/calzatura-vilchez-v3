# Word lock files (created while a .docx is open)
~$*

# Mobile signing keys (never commit)
artifacts/signing/
*.p12
*.mobileprovision

# APK binaries — superan 100MB, usar GitHub Releases o LFS
artifacts/apk/*.apk
artifacts/apk/*.rar
calzatura-vilchez/supabase/.temp/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/
**/.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
coverage.xml
ai-service/coverage*.xml
ai-service/coverage_raw.xml
eslint-report.json
bandit-report.json
.coverage
.scannerwork/
**/.scannerwork/

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Local AI/editor assistant settings
.claude/
.idea/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.*.local

# Build outputs and local platform state
dist/
dist-ssr/
.vercel/

# Python runtime/cache and local credentials
__pycache__/
*.py[cod]
*.pyo
*.pyd
ai-service/venv312/
ai-service/evaluation_report_*.txt
serviceAccountKey.json
*-firebase-adminsdk-*.json

# PHP/vendor dependencies generated locally
tdd/vendor/
**/tdd/vendor/
**/.phpunit.cache/

# Firebase starter output not used by this app (no commitear; borrar local si aparece)
n/

# Artefactos locales de planes / builds móvil
docs/*.xlsx
calzatura-vilchez-mobile/android/build/

# Local screenshots and manual verification artifacts
emulator-home-check.png

# k6 load tests — secretos locales y reportes
load-tests/config.env
artifacts/load-tests/*.json
artifacts/load-tests/*.html

# dataconnect generated files
.dataconnect
